# 第5章「深淵の洞」

黄昏の塔をクリアすると開く、地下2階層の洞窟ダンジョン。

## 敵グラフィック

| ファイル | 敵の名前 | 出る場所 | HP / 攻 / 守 | 備考 |
|---|---|---|---|---|
| `enemy-crystal-boar.png` | 晶牙のガロウ | 地下1階 | 540 / 68 / 30 | ためてから突進 |
| `enemy-crimson-bat.png` | 緋翼のヴェイル | 地下1階 | 420 / 72 / 18 | 最速。脆いが手数が多い |
| `enemy-crystal-lizard.png` | 紫晶のドレイク | 地下2階 | 720 / 80 / 34 | 硬い |
| `enemy-thunder-knight.png` | 碧焔の巨兵 | 地下2階 | 880 / 86 / 44 | 雑魚最強。ボス級の行動をする |
| `enemy-cave-boss-dragon.png` | 深淵のアズヴァル | 地下2階 最奥 | 1800 / 78 / 46 | 章ボス |

取り込み時にやったこと:

- 連結成分を数えて、本体から離れた欠片を除去（コウモリの下の火花、
  リザードの右下の爪、巨兵の右上の翼の断片など。最大で37個）
- 中身に合わせてトリミング
- 表示倍率 `art` を 1.0〜1.35 で振り、見た目で強さの序列が分かるようにした

## ダンジョン

タイルは塔と共通で `0`通路 `1`壁 `2`町へ `3`ボス `4`回復 `u`奥へ `d`戻る
`c`宝箱 `p`落とし穴、これに `g`光苔を追加した。

- **迷路** — 地下1階・2階とも、乱数で掘った迷路に環をいくつか足したもの。
  行き止まりだらけだと歩くのが苦痛なので、壁を4〜5枚抜いて回り道を作ってある
- **落とし穴** — 塔では上から落ちるので階層番号が減るが、洞では深くなる。
  `DUNGEONS[章].pitStep` で向きを持たせた。避けて通れることは
  配置時に到達判定で確認済み
- **暗闇** — 地下2階は灯りの届く範囲しか見えない（`DUNGEONS[5].dark`）。
  階層表示だけは闇の上から描き直している
- **光苔** — 踏むと24歩のあいだ視界が洞ぜんたいに広がる。歩くたびに減る

## 多層ダンジョンの足し方

第4章の塔専用だった仕組みを `DUNGEONS` という登録簿にまとめ直した。
章を足すときは `game/content.js` に1行増やすだけでよい。

```js
5: Object.freeze({
  floors, maps, encounters, chests, landing,
  theme: 'cave',    // 石組みの配色 (FLOOR_PALETTE)
  pitStep: 1,       // 落とし穴でどちらへ動くか
  descend: true,    // 階層番号が大きいほど地下。階段の絵と表記が反転する
  dark: { 2: 3 },   // その階の視界半径 (タイル数)
  mossSteps: 24,
}),
```

`main.js` 側で章番号を見ている箇所は、フィールドの文言と戦闘背景だけ。
